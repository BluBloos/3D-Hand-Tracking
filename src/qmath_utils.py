# TODO: Given the 3D keypoints from MANO output, project these as 2D keypoints.
def PROJECT_3D2D(keyp, camera):
  return keyp

# Given alpha, R^23 joints map to theta, R^45 mano poses.
# This is simply done with some matrix T.
def JOINTS_TO_MANO(joints):
  return joints